---
title: Head Count
---

{% import "face-awe.njk" as svg %}
{% from "macros/button.njk" import button %}

{% extends "base.njk" %}
{% block content %}
<div x-data="{isScrollTop : true, showCount : false}" class="fixed inset-x-0 top-0 z-50 py-6 transform" :class="{'bg-white' : !isScrollTop}">
<header :class=" showCount ? 'grid-rows-2' : 'grid-rows-1'"  @scroll.window = " isScrollTop = (window.pageYOffset > 50) ? false : true" class="container grid justify-between w-full grid-cols-1 gap-2 px-4 text-4xl md:grid-rows-1 auto-cols-auto auto-rows-fr text-brown-500 sm:text4xl">
<nav class="col-start-1 row-start-1">
  <h2 class="grid justify-start grid-flow-col gap-1 leading-tight font-headline sm:leading-normal"><a class="grid justify-start grid-flow-col gap-1 hover:text-brown-700" href="/"><span x-ref="head" data-text="head" class="relative head">head</span><span data-text="count" class="count">count</span></a>
  <a @mouseenter="$refs.head.classList.add('hovered')" @mouseleave="$refs.head.classList.remove('hovered')" data-text="hands" class="relative underline hands text-brown-300" href="/count">hands</a>
  </h2>
</nav>
<aside class="contents">
  <button @click=" showCount = !showCount" class="self-center col-start-2 col-end-3 row-start-1 p-1 rounded-full bg-brown-500 hover:bg-brown-300 focus:outline-none focus:bg-brown-300 focus:ring-2 focus:ring-inset focus:ring-red-500 justify-self-center">{{ svg.face("h-10 sm:h-10") }}</button>
  <div x-show="showCount" :class=" isScrollTop ? 'text-hospital-500' : 'text-white'"  class="inline-flex items-center justify-center col-start-1 col-end-3 row-start-2 px-3 overflow-y-hidden text-sm text-center uppercase rounded sm:justify-between self-baseline bg-brown-500 md:row-start-1 md:col-start-1 md:col-end-2 md:justify-self-end md:leading-normal" ><span class="mr-3 -my-32 transform -translate-y-2 font-headline text-8xl md:text-9xl">10</span> heads and counting</div>
</aside>
</header>
</div>
<main>
<div class="container relative flex flex-wrap justify-center h-screen px-4 mt-40">

  <div class="z-10 flex flex-col items-center justify-center w-full transform md:w-2/3">
  <div class="w-full text-center md:w-2/3 text-brown-500">
  <h1 class="mb-4 text-6xl uppercase 2xl:text-8xl font-headline">head count</h1>
<p class="text-xl font-brand sm:text-2xl lg:text-xl 2xl:text-4xl">Remebember group shots? If you ever wanted to count the number of people in a pic.</p>
{{ button("start counting") }}
  </div>
  </div>
  <div class="z-10 flex justify-center w-full transform lg:justify-start md:w-1/3">
  <div class="flex justify-center w-2/3 md:w-1/2">
    {% include "phone.svg" %}
  </div>
  </div>
 <div class="absolute inset-x-0 bottom-0 z-0 flex h-full -mx-0 transfrom ">
    <img class="object-cover object-bottom w-full h-full" src="https://res.cloudinary.com/makingthings/image/upload/t_duotone-green-brand/v1607877764/headcounthands/masked-medium.png" alt="">
  </div> 
</div>
<div class="container flex flex-col items-center w-full my-8">
<h2 class="uppercase mb-7 text-brown-500 text-9xl font-headline">why?</h2>
<p class="mb-16 text-2xl text-center font-brand text-brown-500">Why might you need to count the numebr of peopl ein  pictue. You may well ask</p>
<section class="grid justify-between w-full grid-cols-2 grid-rows-2 gap-8 mx-auto">
{% for question in questions %}
<article  class="relative w-2/3 p-5 my-12 text-lg rounded bg-brown-500 text-hospital-500 md:text-xl">
<div class="absolute bottom-0 {% if loop.index%2 === 0%}right-full{% else %}left-full{% endif %}   ">
 {% if loop.index%2 === 0%}{{ svg.flippedX("h-6") }} {% else %}{{ svg.flippedY("h-6") }} {% endif %}   {
</div>
<h2 class="mb-6 text-4xl tracking-wider uppercase font-headline uppercas">{{ question.title }}</h2>
<p>{{ question.content }}</p>
</article>
{% endfor %}
</section>
</div>
</main>
</div>
{% endblock %}
